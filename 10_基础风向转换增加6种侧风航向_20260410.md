# 10_基础风向转换增加6种侧风航向_20260410.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (补充需求/常规修改)
- **描述**: 用户要求在“基础转换”的结果显示区域中，除了现有的“逆风航向角”和“顺风航向角”之外，继续补充列出其余 6 种侧风条件下的飞行航向角。
  - 需要增加的内容包括：正左前侧风、正右前侧风、正左侧风、正右侧风、正左后侧风、正右后侧风。

## 2. 问题根源分析
- 目前 `index.html` 中的 `#wind-form-basic` 结果区域只有两个 `div` 用于显示顺/逆风。
- `app.js` 的转换逻辑只计算了这两个角度，没有将八面风的其他六个角度也计算并输出到 UI。

## 3. 查找问题根源的路径和办法
- **修改 HTML**: 在 `<div class="result">` 下新增 6 个 `<div>` 行，赋予对应的 `id`（例如 `wind-lf-angle`, `wind-rf-angle` 等）。
- **修改 JS**: 在 `windCalcBtn` 点击事件的回调中，读取 `windDir`，然后利用之前整理的侧风相对角度公式计算出各方向航向角：
  - 正左前侧风: `(windDir + 45) % 360`
  - 正右前侧风: `(windDir - 45 + 360) % 360`
  - 正左侧风: `(windDir + 90) % 360`
  - 正右侧风: `(windDir - 90 + 360) % 360`
  - 正左后侧风: `(windDir + 135) % 360`
  - 正右后侧风: `(windDir - 135 + 360) % 360`
- 格式化输出。

## 4. 解决问题的策略和方法
1.  **修改 UI (`index.html`)**:
    - 在现有的 `<div>顺风航向角...</div>` 之后追加：
      ```html
      <div>正左前侧风飞行航向(°)：<span id="wind-lf-angle">-</span></div>
      <div>正右前侧风飞行航向(°)：<span id="wind-rf-angle">-</span></div>
      <div>正左侧风飞行航向(°)：<span id="wind-l-angle">-</span></div>
      <div>正右侧风飞行航向(°)：<span id="wind-r-angle">-</span></div>
      <div>正左后侧风飞行航向(°)：<span id="wind-lb-angle">-</span></div>
      <div>正右后侧风飞行航向(°)：<span id="wind-rb-angle">-</span></div>
      ```
2.  **修改逻辑 (`app.js`)**:
    - 在 `#wind-calc` 事件处理中获取上述元素并填入计算值。

## 5. 修改和增加的文件
- `index.html`: 新增结果展示项。
- `app.js`: 新增相应的计算与 DOM 赋值代码。

## 6. 修改的代码
### index.html
在 `#wind-form-basic` 的 `.result` 容器内追加了 6 个占位符：
```html
<div>正右前侧风飞行航向(°)：<span id="wind-rf-angle">-</span></div>
<div>正左前侧风飞行航向(°)：<span id="wind-lf-angle">-</span></div>
<div>正右侧风飞行航向(°)：<span id="wind-r-angle">-</span></div>
<div>正左侧风飞行航向(°)：<span id="wind-l-angle">-</span></div>
<div>正右后侧风飞行航向(°)：<span id="wind-rb-angle">-</span></div>
<div>正左后侧风飞行航向(°)：<span id="wind-lb-angle">-</span></div>
```

### app.js
在 `#wind-calc` 的点击事件中，提取 `windDir` 后分别根据其相对角度计算各侧风方向下的飞行航向：
```javascript
// 侧风航向角计算
document.getElementById('wind-rf-angle').textContent = format2((windDir - 45 + 360) % 360);
document.getElementById('wind-lf-angle').textContent = format2((windDir + 45) % 360);
document.getElementById('wind-r-angle').textContent = format2((windDir - 90 + 360) % 360);
document.getElementById('wind-l-angle').textContent = format2((windDir + 90) % 360);
document.getElementById('wind-rb-angle').textContent = format2((windDir - 135 + 360) % 360);
document.getElementById('wind-lb-angle').textContent = format2((windDir + 135) % 360);
```

## 7. 修改的功能
- “风向转换航向角” -> 基础转换 -> 增加全部 6 面侧风对应的航向输出。

## 8. 修改的模块
- HTML DOM 结构 (`#wind-form-basic .result`)
- JS 事件监听 (`#wind-calc`)

## 9. 修改的接口
- 无。

## 10. 修改的数据结构
- 无。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak10`, `backup/app.js.bak10`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `d362678a79978ec83d539ef35477ce1633ec0256`
- 回滚操作：`git reset --hard d362678` 或使用备份文件覆盖。
