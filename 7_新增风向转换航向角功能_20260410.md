# 07_新增风向转换航向角功能_20260410.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (新增功能)
- **描述**: 用户要求增加一个新功能“风向转换航向角”。用户通过下拉菜单选择风向，系统自动算出对应的“顺风航向角”和“逆风航向角”。
  - **历史记录**: 这是一个全新的需求。

## 2. 问题根源分析
- 现有的项目中没有涉及到气象/风向相关的计算模块。
- 需要梳理风向与航向的航空/航海常识对应关系：
  - **风向**：是指风**吹来**的方向（如北风是 0°，从北向南吹）。
  - **逆风航向**（顶风）：飞行器迎着风飞行的方向。因此，逆风航向 = 风向（如迎着北风飞，航向为 0°）。
  - **顺风航向**（尾风）：飞行器顺着风飞行的方向。因此，顺风航向 = 风向 + 180°（如顺着北风飞，航向为 180° 向南飞）。

## 3. 查找问题根源的路径和办法
- 在 `index.html` 的侧边栏导航中增加一个新的菜单按钮，并添加对应的数据面板 `<section id="calc-wind">`。
- 面板中提供一个 `<select>` 下拉菜单，包含常见的风向选项及其对应的角度值。
- 在 `app.js` 中增加监听下拉菜单变化（或者点击计算按钮）的事件，提取出角度值，分别计算并更新页面显示。

## 4. 解决问题的策略和方法
1.  **UI 结构 (`index.html`)**:
    - 侧边导航新增：`<button class="nav-item" data-target="calc-wind">风向转换航向角</button>`
    - 主区域新增面板，包含 `<select id="wind-dir-select">`，选项如“北风 (0°)”、“东北风 (45°)”等（16个方位或8个主要方位均可，这里为了全面使用16方位或8方位，我们采用8方位+支持自定义输入的下拉框，但最简单的是只用预设的8个主要风向：0, 45, 90, 135, 180, 225, 270, 315）。
2.  **计算逻辑 (`app.js`)**:
    - 获取选中风向的数值 `wd`。
    - 逆风航向角 = `wd`。
    - 顺风航向角 = `(wd + 180) % 360`。
    - 渲染到 DOM 中的 `<span id="wind-head-angle">` 和 `<span id="wind-tail-angle">`。

## 5. 修改和增加的文件
- `index.html`: 新增模块结构。
- `app.js`: 新增交互逻辑。

## 6. 修改的代码
### index.html
在左侧导航栏 `.nav` 中增加了一个新按钮：
```html
<button class="nav-item" data-target="calc-wind">风向转换航向角</button>
```
在右侧内容区 `.content` 中增加了一个新面板 `<section id="calc-wind" class="view">`，内部包含一个带有 8 个主要风向选项的 `<select>` 下拉框和显示结果的 `<span>` 元素。

### app.js
为新增的按钮 `wind-calc` 添加了点击事件监听器：
```javascript
const windCalcBtn=document.getElementById('wind-calc');
if(windCalcBtn){
  windCalcBtn.addEventListener('click', ()=>{
    const windDir = parseFloat(document.getElementById('wind-dir-select').value);
    if(!isFinite(windDir)) return;
    
    // 逆风航向角 = 风向
    const headAngle = windDir;
    // 顺风航向角 = 风向 + 180
    let tailAngle = (windDir + 180) % 360;
    
    document.getElementById('wind-head-angle').textContent = format2(headAngle);
    document.getElementById('wind-tail-angle').textContent = format2(tailAngle);
  });
}
```

## 7. 修改的功能
- 导航菜单 -> 增加“风向转换航向角”功能。

## 8. 修改的模块
- HTML DOM 结构
- JS 事件监听和计算逻辑

## 9. 修改的接口
- 无。

## 10. 修改的数据结构
- 无。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak7`, `backup/app.js.bak7`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `05fc6ded23eaabb583932b881e1e7e3a918ea1bf`
- 回滚操作：`git reset --hard 05fc6de` 或使用备份文件覆盖。
