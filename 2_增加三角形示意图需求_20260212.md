# 02_三角形计算器增加图形实例_20260212.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (增加功能)
- **描述**: 用户要求在“三角形计算器”中增加图形实例，以便更直观地理解对应角和对应边的含义。
  - **历史记录**: 这是第一次提出该需求。

## 2. 问题根源分析
- 当前界面只有文字标签（如“角A”、“边1”、“两边夹角”），对于不熟悉几何符号的用户来说，可能难以快速对应到具体的三角形形状上。
- 缺少直观的视觉反馈，用户在选择“边1对角”或“两边夹角”时，只能凭空想象。

## 3. 查找问题根源的路径和办法
- 检查 `index.html`，确认 `#calc-triangle` 区域目前只有输入表单和结果区域。
- 检查 `style.css`，确认没有相关的图形样式。

## 4. 解决问题的策略和方法
1.  **修改 UI (`index.html`)**:
    - 在三角形计算器区域（`#calc-triangle`）顶部或合适位置，添加一个 SVG 示意图。
    - SVG 中绘制一个标准三角形，标注 A, B, C 角和 a, b, c 边。
    - 为 SVG 中的元素（角、边）添加 ID 或 Class，以便通过 JS 控制高亮。
2.  **修改样式 (`style.css`)**:
    - 设置 SVG 的基本样式（大小、居中、边框颜色等）。
    - 定义高亮样式（例如 `active` 类），使选中的边或角显示为红色或加粗。
3.  **修改逻辑 (`app.js`)**:
    - 监听用户在“已知两角一边”、“两边及任意一角”、“已知三边解角”Tab 的切换，以及具体的输入框聚焦或单选框选择事件。
    - 根据当前模式和用户选择，动态为 SVG 中的对应元素添加高亮类。
    - 例如：
        - 选中“两边夹角”时，高亮角 A 以及构成该角的两边 b, c（假设模型如此对应）。
        - 选中“边1对角”时，高亮角 A，边 a (对边) 和边 b (邻边)。

## 5. 修改和增加的文件
- `index.html`: 增加 SVG 结构。
- `style.css`: 增加图形样式。
- `app.js`: 增加交互逻辑。

## 6. 修改的代码
### index.html
新增 `.tri-visual` 区域，包含 SVG 图形，绘制了三角形 A-B-C 及其对应的边 a, b, c，并为每个元素设置了 `data-id`。

### style.css
新增 SVG 相关样式，包括默认样式和 `.active` 高亮样式（红色边框、填充等）。

### app.js
新增 `updateTriVisual` 函数，并在 Tab 切换和单选框变更时调用。该函数根据当前计算模式（ASA/SAS/SSS）和选择的具体条件，通过操作 DOM 的 `classList` 来高亮对应的边和角。

## 7. 修改的功能
- 三角形计算器 -> 增加动态示意图。

## 8. 修改的模块
- HTML DOM 结构 (`#calc-triangle`)
- CSS 样式
- JS 事件监听

## 9. 修改的接口
- 无后端接口修改。

## 10. 修改的数据结构
- 无持久化数据结构修改。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak2`, `backup/style.css.bak2`, `backup/app.js.bak2`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `40871fe15e360684f01665698bbb3c8669e5bde6`
- 回滚操作：`git reset --hard 40871fe` 或使用备份文件覆盖。
