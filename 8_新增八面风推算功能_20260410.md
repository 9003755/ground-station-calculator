# 08_新增八面风推算子功能_20260410.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (新增子功能)
- **描述**: 用户要求在“风向转换航向角”功能中增加一个“八面风推算”功能。提供一个类似于图例的飞机和八面风向图（SVG）。用户可以选择“风向”、“航向”、“侧风条件”中的任意两个已知条件，系统自动算出第三个未知条件。
  - **示例**: 风向 SW(225°), 航向 S(180°), 自动算出条件为“正右前侧风”。

## 2. 问题根源分析
- 目前的风向模块只支持简单的顺风/逆风转换，无法满足复杂的“三知二求一”推算逻辑，且缺乏八面风向的图形化展示。
- 需要将公式抽象化：`航向 = (风向 + 侧风条件相对角度) % 360`。
  - 逆风: +0°
  - 正左前侧风: +45°
  - 正左侧风: +90°
  - 正左后侧风: +135°
  - 顺风: +180° / -180°
  - 正右后侧风: -135°
  - 正右侧风: -90°
  - 正右前侧风: -45°

## 3. 查找问题根源的路径和办法
- **HTML 结构**: 将 `#calc-wind` 面板划分为两个 Tab，一个是现有的“基础转换”，另一个是新增的“八面风推算”。
- **SVG 图例**: 在新 Tab 的顶部使用 `<svg>` 绘制一架飞机和八个方向的箭头及文字，呈现出直观的八面风模型。
- **表单**: 提供三个 `<select>` 菜单，分别对应风向、航向、侧风条件，每个菜单都有一个“--未知--”选项。
- **JS 逻辑**: 点击“推算”时，校验是否刚好有两个输入项有值，一个为空。然后根据已知条件通过公式解方程算出未知量，最后更新显示。

## 4. 解决问题的策略和方法
1.  **修改 UI (`index.html`)**:
    - 在 `#calc-wind` 中加入 `.tabbar`，包含 `基础转换` 和 `八面风推算` 两个标签。
    - 添加图例 SVG 代码，利用 `<g transform="rotate(deg)">` 来渲染八个不同角度和颜色的风向箭头。
    - 添加 3 个选择框。
2.  **修改逻辑 (`app.js`)**:
    - 更新现有的 Tab 切换监听器，使其兼容新增的风向 Tab（通过 `data-wind-tab` 与 `data-tri-tab` 区分）。
    - 增加八面风计算逻辑，利用代数关系 `Heading - Wind = Offset` 解答未知项。

## 5. 修改和增加的文件
- `index.html`: 新增风向推算 Tab 及 SVG 图例。
- `app.js`: 增加交互逻辑和数学计算。

## 6. 修改的代码
### index.html
重构了 `#calc-wind` 区域，加入了 `.tabbar` 并分出两个子面板：
1. `#wind-form-basic`：原来的基础转换逻辑。
2. `#wind-form-eight`：新增的八面风推算模块。
   - 顶部是一个 `<svg>` 图形，绘制了中心飞机、环形虚线以及 8 个带有对应颜色和方向的箭头与文字标签。
   - 中部是三个 `<select>` 选择框，分别对应“风向”、“航向”和“侧风条件”。
   - 底部是“推算”按钮和结果展示区域。

### app.js
1. 升级了 Tab 切换监听器逻辑，通过判断 `dataset.windTab` 和 `dataset.triTab`，让同一个 `.tabbar` 切换逻辑能平滑兼容两个不同页面的选项卡。
2. 新增 `#eight-calc` 点击事件：
   - 收集 3 个选择框的值。
   - 校验是否刚好有 1 个空值（未知量）。
   - 如果“侧风条件”未知：计算 `offset = Heading - Wind`，通过映射表 `windTypeMap` 得出对应的文字结果（如正右前侧风）。
   - 如果“航向”未知：计算 `Heading = (Wind + offset + 360) % 360`。
   - 如果“风向”未知：计算 `Wind = (Heading - offset + 360) % 360`。

## 7. 修改的功能
- “风向转换航向角”模块 -> 新增“八面风推算”子功能。

## 8. 修改的模块
- HTML DOM 结构 (`#calc-wind`)
- JS 选项卡切换逻辑及推算算法

## 9. 修改的接口
- 无。

## 10. 修改的数据结构
- 无。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak8`, `backup/app.js.bak8`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `ff237613e342ce64bf0a5756467f2656cc112809`
- 回滚操作：`git reset --hard ff23761` 或使用备份文件覆盖。