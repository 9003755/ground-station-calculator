# 03_新增时钟夹角计算器功能_20260328.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (重要的新增功能)
- **描述**: 用户要求在计算器中增加“时针分钟计算夹角”的功能。用户输入时间（小时和分钟），程序自动算出该时刻时针和分针之间的夹角。
  - **历史记录**: 这是一个新的需求，之前没有提过。

## 2. 问题根源分析
- 目前项目包含“简易计算器”、“三角形计算器”、“航向角计算器”、“任意两点间航向和距离计算器”和“速度单位换算”。
- 缺少专门用于处理时钟角度计算的模块。

## 3. 查找问题根源的路径和办法
- 需要在左侧导航栏 (`.sidebar`) 新增一个菜单项。
- 需要在右侧内容区 (`.content`) 新增一个对应的面板。
- 需要根据用户提供的图片逻辑（$|30 \times H - 5.5 \times M|$ 等）在 `app.js` 中实现算法。

## 4. 解决问题的策略和方法
1.  **修改 UI (`index.html`)**:
    - 侧边栏增加 `<button class="nav-btn" data-target="calc-clock">时钟夹角计算器</button>`。
    - 内容区增加 `<div id="calc-clock" class="panel" style="display:none;">`。
    - 面板内包含：小时输入框 (0-23或1-12皆可，为通用起见，采用24小时制转12小时制逻辑)，分钟输入框 (0-59)，计算按钮，以及结果展示区（显示时针角度、分针角度、两针夹角）。
2.  **修改逻辑 (`app.js`)**:
    - 在现有的导航切换逻辑中，使新的面板可以正常显示。
    - 新增计算事件监听器：
        - 读取输入的小时 $H$ 和分钟 $M$。
        - 转换 $H$ 到 12 小时制 ($H = H \pmod{12}$)。
        - 计算时针角度: $Angle_H = 30 \times H + 0.5 \times M$
        - 计算分针角度: $Angle_M = 6 \times M$
        - 计算夹角: $Diff = |Angle_H - Angle_M|$，如果 $Diff > 180$，则取 $360 - Diff$ 得到最小夹角（根据图片公式）。
        - 更新 UI 结果显示。

## 5. 修改和增加的文件
- `index.html`: 新增侧边栏菜单和对应的计算面板。
- `app.js`: 新增时钟角度的计算和事件绑定逻辑。

## 6. 修改的代码
### index.html
新增侧边栏按钮和对应的面板结构：
```html
<button class="nav-item" data-target="calc-clock">时钟夹角计算器</button>

<section id="calc-clock" class="view">
  <div class="form">
    <div class="grid-2">
      <label>小时(0-23)<input type="number" id="clock-h" step="1" min="0" max="23"></label>
      <label>分钟(0-59)<input type="number" id="clock-m" step="1" min="0" max="59"></label>
    </div>
    <button id="clock-calc">计算</button>
  </div>
  <div class="result">
    <div>时针指向角度(°)：<span id="clock-h-angle">-</span></div>
    <div>分针指向角度(°)：<span id="clock-m-angle">-</span></div>
    <div>时针与分针夹角(°)：<span id="clock-diff-angle">-</span></div>
  </div>
</section>
```

### app.js
新增了对应的事件绑定和计算逻辑：
- 时针角度 = 30 * (H % 12) + 0.5 * M
- 分针角度 = 6 * M
- 夹角 = min(|Angle_H - Angle_M|, 360 - |Angle_H - Angle_M|)

## 7. 修改的功能
- 新增“时钟夹角计算器”功能模块。

## 8. 修改的模块
- HTML DOM 结构 (`.sidebar`, `.content`)
- JS 事件监听和计算逻辑

## 9. 修改的接口
- 无后端接口修改。

## 10. 修改的数据结构
- 无持久化数据结构修改。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak3`, `backup/app.js.bak3`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `c9eed4f76ee3726a26e9c758fcc25ec773b841de`
- 回滚操作：`git reset --hard c9eed4f` 或使用备份文件覆盖。
