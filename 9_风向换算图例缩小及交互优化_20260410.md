# 09_风向换算图例缩小及交互优化_20260410.md

## 1. 问题和需求描述
- **类型**: B. 功能设计/UI设计不符合/没满足你的需求 (常规修改)
- **描述**: 
  1. 在基础换算中，给风向下拉菜单的选项增加英文缩写（如 N, NE 等）。
  2. 在八面风推算中，将顶部图例缩小 30%。
  3. 图例初始状态为空白（隐藏所有侧风箭头和文字）。当用户在下拉框中输入已知条件后，图例自动且直观地显示对应的条件图示（对应的风向箭头及文字）。

## 2. 问题根源分析
- 目前 `<select id="wind-dir-select">` 选项只有中文和度数。
- SVG 设置的 `max-width: 360px` 有点过大，占用过多垂直空间。
- 八面风的 8 个箭头及文字目前是硬编码一直显示，没有动态交互效果。

## 3. 查找问题根源的路径和办法
- **修改选项**: 找到 `#wind-dir-select`，把 `<option>` 的文本改为“北风(N/0°)”的形式。
- **缩小图例**: 将 `max-width: 360px` 调整为 `250px`（大约缩小 30%）。
- **动态交互**: 给 SVG 中的每个箭头 `<g>` 和标签 `<text>` 添加 `class="wind-arrow"` / `class="wind-text"` 和标识对应角度的 `data-offset="X"`，并添加 `opacity: 0; transition: opacity 0.3s` 让其默认隐藏且带有淡入动画。在 JS 中监听三个 `<select>` 的 `change` 事件，如果已知条件足够确定“侧风条件”对应的角度（`offset`），则将其对应的 `opacity` 改为 1。

## 4. 解决问题的策略和方法
1.  **UI 结构 (`index.html`)**:
    - 更新 `#wind-dir-select` 的 `<option>` 文本。
    - 将 `<svg>` 的 `max-width` 从 `360px` 降至 `250px`。
    - 修改 SVG `<g>` 组和 `<text>` 组，为对应的 8 个方位增加 `class`、`data-offset` 和 `style="opacity:0"`。
2.  **计算逻辑 (`app.js`)**:
    - 新增函数 `syncEightVisual(offset)` 用于控制 DOM 节点的 `opacity` 切换。
    - 绑定三个下拉框的 `change` 事件：当任意值改变时，实时尝试计算 `offset`，如果可以确定就调用 `syncEightVisual(offset)` 显示指定箭头，否则隐藏所有。

## 5. 修改和增加的文件
- `index.html`: 修改选项文本和 SVG 属性。
- `app.js`: 增加实时联动图例的逻辑。

## 6. 修改的代码
### index.html
1. 在 `#wind-form-basic` 内部更新了 `#wind-dir-select` 的 `<option>`，加上了如 `北风(N/0°)` 这样的缩写。
2. 将 `#wind-form-eight` 内 `<svg>` 元素的 `max-width` 修改为了 `250px`。
3. 为 SVG 内部的每个方向箭头组 `<g>` 和文字 `<text>` 加上了 `class="wind-arrow"` / `class="wind-text"`、`data-offset="X"` 属性，以及默认样式 `style="opacity:0; transition:opacity 0.3s"`。

### app.js
1. 增加了 `syncEightVisual(offset)` 函数，用于隐藏所有箭头/文字，并将与计算出 `offset` 相匹配的元素 `opacity` 设为 `1`。
2. 增加了 `updateVisualFromSelects()` 提取三个下拉框的值实时进行判断。如果满足任意两个条件（尤其是侧风条件），就能直接展示正确的图例箭头。
3. 绑定这三个 `<select>` 的 `change` 事件，使得用户在下拉选择条件时，即使没点“推算”按钮，也能得到实时的图形反馈。点击“推算”时同样触发一次以确保状态同步。

## 7. 修改的功能
- 优化了风向计算的 UI 展示与交互体验。

## 8. 修改的模块
- HTML DOM 结构 (`#wind-dir-select`, `#wind-form-eight svg`)
- JS 实时事件响应

## 9. 修改的接口
- 无。

## 10. 修改的数据结构
- 无。

## 11. 备份策略
- 已创建本地文件备份：`backup/index.html.bak9`, `backup/app.js.bak9`

## 12. Git 提交点和回滚标签
- 提交前 Hash: `5828f1a59e953f5da10bc123b681043cba5feabc`
- 回滚操作：`git reset --hard 5828f1a` 或使用备份文件覆盖。
